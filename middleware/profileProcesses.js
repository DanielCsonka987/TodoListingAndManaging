let mongoose = require('mongoose');
let dbUrl = require('../config/appConfig.js').dbAccess;

module.exports.reviseProfilePassword = (profileId, profilePwdString)=>{

};

module.exports.createProfile = (profile) =>{

};

module.exports.changeProfilePassword = (profileId, newPwdString) =>{

}

module.exports.deleteProfile = (profileId)=>{

};
